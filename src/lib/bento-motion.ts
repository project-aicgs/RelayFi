/** Shared scroll + motion tuning for Figma bento cards. */
export const bentoScrollThreshold = 0.52;
export const bentoScrollRootMargin = "-20% 0px";

/** Base 0.55 → +25% → +30% more = 0.89375s. */
export const bentoMotionDuration = 0.6875 * 1.3;

/** Click spark pop (post-mortem hover). */
export const bentoClickSparkDuration = 0.42;
